{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/Dockerfile.ops"
  },
  "deploy": {
    "cronSchedule": "0 7 * * *",
    "restartPolicyType": "NEVER"
  }
}
